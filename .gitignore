# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Secrets (none are used by this project; keep it that way)
.env
.env.*
*.pem
*.key

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Note: data/, output/ and docs/*.png are committed on purpose so the
# repository shows real, reproducible results (`python main.py` regenerates them).
