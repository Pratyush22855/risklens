[project]
name = "risklens"
version = "1.0.0"
description = "Explainable transaction security risk engine built on synthetic data"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = ["pandas>=2.0", "numpy>=1.26", "matplotlib>=3.8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
